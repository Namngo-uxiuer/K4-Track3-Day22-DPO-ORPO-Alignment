# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.matplotlib-cache/

# Jupyter
.ipynb_checkpoints/
# NOTE: notebooks/*.ipynb IS committed for submission (with output cells preserved
# for grading). The Jupytext `.py` files are the source-of-truth template;
# `.ipynb` is the student's executed deliverable.

# Lab artifacts (regenerated on every run; do not commit)
data/pref/
data/eval_prompts.json
adapters/sft-mini/
adapters/dpo/
adapters/orpo/
gguf/
*.gguf
runs/
wandb/

# HuggingFace cache (Colab + local — model weights are huge)
.cache/
hf_cache/

# Env / secrets
.env
.env.local
*.token

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Submission evidence (keep lightweight configs, metrics, data and screenshots
# visible to GitHub; choose Option C if the adapter weights should stay local).
!data/pref/
!data/pref/*.parquet
!data/pref/*.json
!data/pref/*.md
!data/eval/
!data/eval/*.json
!data/eval/*.jsonl
!data/eval/*.txt
!adapters/
!adapters/sft-mini/
!adapters/sft-mini/*.json
!adapters/sft-mini/*.md
!adapters/sft-mini/*.jinja
!adapters/sft-mini/tokenizer.json
!adapters/sft-mini/tokenizer_config.json
!adapters/dpo/
!adapters/dpo/*.json
!adapters/dpo/*.md
!adapters/dpo/*.jinja
!adapters/dpo/tokenizer.json
!adapters/dpo/tokenizer_config.json
adapters/sft-mini/adapter_model.safetensors
adapters/dpo/adapter_model.safetensors
adapters/sft-mini/tokenizer.json
adapters/dpo/tokenizer.json
!submission/screenshots/
!submission/screenshots/*.png
!gguf/
!gguf/README.md
